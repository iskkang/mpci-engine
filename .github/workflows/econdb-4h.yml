name: EconDB 4h Fetch (DISABLED)

# ⚠️ 이 워크플로우는 fetch_econdb.yml 로 대체되어 비활성화됨
# fetch_econdb.py가 --job 인수를 요구하도록 변경되어 스케줄 실행 불가

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '이 워크플로우는 deprecated입니다. fetch_econdb.yml을 사용하세요.'
        required: true
        default: 'cancel'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "이 워크플로우는 비활성화되었습니다. fetch_econdb.yml을 사용하세요."
